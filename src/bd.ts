import { Pool } from 'pg';

const pool = new Pool({
  user: 'postgres',
  host: '176.57.150.47',
  database: 'postgres',
  password: 'GWoxaAV]202420248bMz[789M:?JW',
  port: 5559,
});

export default pool;
